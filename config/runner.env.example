# Copy to config/runner.env and edit on the server.

export CODEX_BIN=codex

# Optional:
# export CODEX_MODEL=gpt-5-codex
# export CODEX_PROFILE=default

# 0 means run forever.
export CODEX_MAX_ROUNDS=0
export CODEX_SLEEP_SECONDS=10
export CODEX_MAX_CONSECUTIVE_FAILURES=3

# read-only | workspace-write | danger-full-access
export CODEX_RUNNER_SANDBOX=workspace-write

# Use only inside an externally isolated VM/container.
export CODEX_DANGER_FULL_ACCESS=0

# Use Codex's low-friction automatic mode instead of explicit sandbox config.
export CODEX_FULL_AUTO=0

# Enable live web search if your Codex installation/account supports it.
export CODEX_ENABLE_SEARCH=0

# Optional raw extra args appended to `codex exec`.
# export CODEX_EXTRA_ARGS='--some-flag'
