# Copy to config/runner.env on an isolated Ubuntu sandbox server when you want
# Codex to run with maximum autonomy.

export CODEX_BIN=codex

# Optional:
# export CODEX_MODEL=gpt-5-codex
# export CODEX_PROFILE=default

# 0 means run forever.
export CODEX_MAX_ROUNDS=0
export CODEX_SLEEP_SECONDS=10
export CODEX_MAX_CONSECUTIVE_FAILURES=5

# Isolated-server max permission mode.
# This implies:
# - --dangerously-bypass-approvals-and-sandbox
# - top-level --search
# - prompt authorization for sudo, installs, scripts, services, monitors
export CODEX_UNRESTRICTED=1

# These are redundant when CODEX_UNRESTRICTED=1, but kept explicit for clarity.
export CODEX_DANGER_FULL_ACCESS=1
export CODEX_ENABLE_SEARCH=1
export CODEX_ALLOW_SUDO=1
export CODEX_ALLOW_INSTALL=1
export CODEX_ALLOW_LONG_RUNNING=1
export CODEX_ALLOW_SCRIPTING=1

# Used only when CODEX_DANGER_FULL_ACCESS=0 and CODEX_FULL_AUTO=0.
export CODEX_RUNNER_SANDBOX=danger-full-access
export CODEX_FULL_AUTO=0

# Optional raw extra args appended to `codex exec`.
# export CODEX_EXTRA_ARGS='--some-flag'
